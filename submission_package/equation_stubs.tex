% Labeled equations referenced from the main text and from tables.
% Kept outside deferred floats so \\ref resolves on the first LaTeX pass.

\begin{equation}
\mathcal{F}_{diss} = \int_0^L \left( \eta_p |\dot{\kappa}|^2 + \eta_a |\nabla I|^2 + \Gamma_m \right)\, ds
\label{eq:dissipation}
\end{equation}

\begin{equation}
\lambda_0(\chi_\kappa, g) \approx \lambda_{C} - \alpha_1 \chi_\kappa + \alpha_2 g
\label{eq:mode_selection}
\end{equation}

\begin{equation}
\InstabilityR = \frac{P_{\mathrm{demand}}}{S_{\mathrm{supply}}}
\label{eq:instability_ratio}
\end{equation}

\begin{equation}
\alpha(s) = \left| \hat{n}_{\mathrm{info}}(s) \cdot \hat{n}_{\mathrm{stress}}(s) \right|
\label{eq:alignment_param}
\end{equation}
