# From Molecules to Curvature: A Causal Map of Scoliosis

The **Scoliosis Mechanism Map** connects molecular actors to tissue mechanics via **Biological Counter-Curvature**, framing scoliosis as a failure of an active control system meant to neutralize gravity. This loop dynamically adjusts structural stiffness ($EI$) and target curvature ($\kappa_0$) via mechanotransductive gain ($G_{mech}$) to maintain spinal stability. When this loop fails, Euler buckling occurs, leading to scoliosis.

---

## Core Pathways

1. **Geometry (HOX)**: `HOX` $\rightarrow$ `Region` $\rightarrow$ `Rest Curvature (\kappa_0)`. Encodes baseline geometry.
2. **Feedback Gain (Piezo)**: `PIEZO1/2` $\rightarrow$ `Mechanotransduction` $\rightarrow$ `Gain (G_{mech})`. Mechanical stretch converts to adaptive growth.
3. **Chiral Initialization (Cilia)**: `CILIA` $\rightarrow$ `Symmetry Breaking` $\rightarrow$ `Torsion (\epsilon)`.
4. **Material Stiffness (Collagen)**: `COL2A1` $\rightarrow$ `ECM Assembly` $\rightarrow$ `Stiffness (EI)`. Resistance to Euler buckling.

---

## Failure Modes

1. **Osteocyte Dendrites**: `PIEZO1` $\rightarrow$ `CCN1/2` $\rightarrow$ `Dendrites` $\rightarrow$ `Gain`.
   > **Hypothesis**: Energy Deficit starves this network, dropping Gain and triggering buckling.
2. **Spinal Jetlag**: `BMAL1` $\rightarrow$ `Circadian Entrainment` $\rightarrow$ `Disc Maintenance`. Loading synchronizes anabolic repair.
3. **CSF-Proprioception Relay**: `PKD2L1` $\rightarrow$ `CSF Sensing` $\rightarrow$ `URP` $\rightarrow$ `Muscle Tone`. Reissner Fiber tension references muscle symmetry.
4. **Glymphatic Stasis**: `VCL Stasis` $\rightarrow$ `Hydraulic Congestion` $\rightarrow$ `MMP`. Fluid stagnation drives annulus degradation.
5. **Epigenetic Asymmetry**: `mtROS` $\rightarrow$ `METTL3` $\rightarrow$ `ESR1` $\rightarrow$ `Muscle Tone`. Unloading stress downregulates methylation.
6. **Piezo-Cilia Stress Lock**: `PIEZO1` $\rightarrow$ `Ciliary Tuning` $\rightarrow$ `Osteogenesis`. Compression destroys cilia, derepressing osteogenesis.
7. **PIEZO1-YAP Degeneration**: `PIEZO1` $\rightarrow$ `YAP` $\rightarrow$ `MMP`. Overload triggers YAP/MMP axis, degrading IVD stiffness (Peng et al., 2025).
8. **Metabolic Bottleneck**: `PPARGC1A` $\rightarrow$ `Mitochondrial Biogenesis` $\rightarrow$ `Energy Deficit`. Energy supply fails to keep up with scaling (Krishnan S et al., 2025).
9. **Osteoblast Defect**: `CILIA` $\rightarrow$ `Actin Dynamics` $\rightarrow$ `VEGF-A` $\rightarrow$ `Gain`. Loss of adaptive gain (Oliazadeh et al., 2022).
   > **Hypothesis**: Defective actin-VEGF-A axis acts as a molecular bottleneck suppressing feedback gain.
10. **Voltage Gain**: `KCNQ` $\rightarrow$ `Mechanotransduction` $\rightarrow$ `Gain`. Hyperpolarization dampens proprioceptive loop (Sánchez-Carranza et al., 2024).
11. **Ferroptosis Axis**: `PIEZO1` $\rightarrow$ `GPX4` $\rightarrow$ `Ferroptosis` $\rightarrow$ `Growth Plate Dysplasia`. Breakdown of antioxidant defense drives asymmetric ossification (Chen et al., 2025).
12. **Stochastic Resonance**: `Muscle Tone` $\rightarrow$ `Micro-Tremor` $\rightarrow$ `PIEZO1/2` $\rightarrow$ `Gain`. Loss of dither signal causes sensory lock-up.
   > **Hypothesis**: Targeted vibration therapy can boost mechanosensor sensitivity.
13. **Caveolin Gain Control**: `CAV1` $\rightarrow$ `Membrane Tension` $\rightarrow$ `YAP`. Loss of membrane buffering causes constitutive YAP nuclear translocation (Moreno-Vicente et al. 2018).
   > **Hypothesis**: CAV1 deficiency lowers the critical buckling load.
14. **Manganese Glycosylation Axis**: `SLC39A8` $\rightarrow$ `Glycosylation` $\rightarrow$ `Stiffness`.
   **Mechanism**: SLC39A8 transports manganese, a cofactor for glycosyltransferases. Loss impairs proteoglycan synthesis.
   **Role**: **Stiffness Deficit**. Defective glycosylation compromises ECM integrity and tissue stiffness.
   **Evidence**: Haller et al. (2018) linked SLC39A8 variants to impaired manganese transport, glycosylation, and severe adolescent idiopathic scoliosis.

---
*Generated by Systems Mapper based on the Biological Counter-Curvature framework.*