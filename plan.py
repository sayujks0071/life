import os
import sys

print("""
The review indicates that the markdown file must be sent to the user as requested by the original prompt. The prompt asked to message the generated report directly to the user.
""")
