# Pre-registered reading of the re-run after the measurement fix (written before the run)

Date: 2026-09-12. Script: `scripts/experiments/newton/ratchet_rod_newton.py` at the commit
that introduces `rod_measure.py`. Old artifacts kept as `*.pre-measurement-fix-2026-09-12.*`.

## What was wrong (two independent defects, both fixed)

1. `joint_dtheta` computed `atan2(px, py)` of body **positions** — the azimuth of each body
   about the vertical — and differenced that. The rod lives in the x–z plane (`py ≈ 0`), so
   this was numerically undefined noise; the ratchet law itself consumed it. The 15,628°
   "Cobb" is this.
2. The permanent set was written to `joint_rod_rest_kb_local[:, 0]` in units of 1/m. Newton's
   rod joint stores the DER curvature binormal (an angle per joint) in the parent-local frame,
   bend about local **y** = component 1 for an x–z column. So the law prescribed rest bending
   about x (the y–z plane, which the x-shake and the gravity sag never enter), 48× too large.

Protocol otherwise unchanged: 9 conditions (`k_r` ∈ {0.3, 1, 6} yr⁻¹ × `B_g` ∈ {0.2, 0.3,
0.5}), `k_g,peak` = 1 yr⁻¹, 180 growth-months, 2 root-shake cycles/month at 4 mm, control run
with the law off.

## Outcomes and what each would mean (decided now, not after)

- **Sanity (must hold or the run is discarded, not interpreted):** Cobb in O(1–40°) for every
  world at every observation; `max |t_y|` (out-of-plane tangent) < 1e-3; control-run Cobb drift
  over 180 months < the 0.5° seed tilt × 10.
- **Gate A2 (control):** with the law off, `|Cobb(20 y) − Cobb(5 y)|` per world must be smaller
  than the law-on permanent set it is compared with. If not → shake-driven mechanics dominate,
  the experiment cannot see growth-driven ratcheting under this shake amplitude; report, do not
  retune the shake to pass.
- **Gate C (the claim under test):** mean |κ_p| at 20 y orders `k_r` 0.3 > 1.0 > 6.0 at every
  `B_g`.
  - PASS at all three `B_g` → the spatially resolved rod reproduces the scalar ratchet's
    ordering; the `rod/ODE` ratio column then says how far the local law departs from the
    scalar one (a ratio ≪ 1 or ≫ 1 is itself the finding, and is reported as is).
  - FAIL with sane Cobb → **a real null**: per-joint cementing driven by local elastic
    curvature does not order by restoration rate under this load protocol. Reported as a
    negative result for the rod extension; the scalar model's claim is unaffected either way
    because the scalar model imposes the ordering by construction (09-08 review §2).
  - Mixed (passes at some `B_g`) → reported per `B_g`; no post-hoc selection of the passing
    `B_g` as "the physiological one".
- Nothing here is clinical. The rod has no vertebrae, discs, muscles, ribs or growth plates.

## Smoke run first

`RATCHET_MONTHS=12 RATCHET_SKIP_A1=1 RATCHET_OUT=<scratch>` — only the sanity block is read
from it; no gate is read from a 12-month run.

## Smoke result (12 months, A1 skipped; read 2026-09-12 13:25, before the full run)

Sanity block only, as pre-registered:
- Cobb (total |dθ| over 23 joints) 10.6–56.2° across the nine worlds — physical, versus
  13,471–15,628° before the fix. `B_g = 0.2` (crit 0.136) reads 36.6 → 54–56° over the 12
  months with a **17.5° control drift**: that world is still settling toward its gravity sag,
  not drifting under the shake (calibration note 3: near-critical columns take ~10³ s of
  simulated time to equilibrate). Whether it settles within 180 months is read from the full
  run; if its control drift there exceeds the 5° bound, `B_g = 0.2` is reported as
  un-equilibrated and excluded from Gate A2/C, not tuned.
- `B_g = 0.3`: control drift 1.13°; `B_g = 0.5`: 0.02°. Both inside the bound.
- `max |t_y|` = 0.00 exactly over both runs: the column stays planar; the x–z measurement
  is valid.
- `kappa_e0` derived from the rod: 1.02 rad/m (was 34.2 rad/m from the broken measurement).
- Not read (pre-registered): Gate C was `True` at all three `B_g` and the rod/ODE ratio
  column sat at 0.01–0.04 in this 12-month run. Neither is evidence until the 180-month run
  reports; the ratio in particular is expected to move because the scalar ODE accumulates
  over the whole window while the rod's local law had 12 months.

Timing: 342 s per 12-month batch → ~85 min per 180-month batch, two batches plus Gate A1.
Full run launched 13:26 IST, detached, logging to `run.log` in this directory.

## Correction to the smoke reading above (2026-09-12, before the full run reports)

The smoke numbers recorded in the previous section were **48× too large**: the observation
summed joint curvature (rad/m) over joints and called the sum degrees without multiplying by the
segment length (0.5/24 m). Found by the Codex continuation
(`reports/newton_readout_audit_2026-09-12/REVIEW.md`), fixed in `rod_measure.total_absolute_bend_deg`
and read back with `scripts/experiments/newton/ratchet_rod_readout.py`, which corrects the
running process's schema-1 output without touching the raw file (source SHA256 12b741f6…).
Corrected smoke values: total absolute planar bend **0.221–1.171°** across the nine worlds
(not 10.6–56.2°); absolute control drift **0.365 / 0.024 / 0.0005°** at B_g 0.2 / 0.3 / 0.5 (not
17.5 / 1.13 / 0.02°). The quantity is total absolute planar bend, not clinical Cobb. The
control drift was also mis-defined (experiment first sample subtracted from control last sample,
negatives clipped); it is now |control last − control first| per world. Observation ages are
end-of-month (recorded window starts at 5.083 y; there is no age-5 baseline sample).

Consequence for the pre-registered sanity block: its "Cobb in O(1–40°)" bound was set against the
2026-09-03 run's inflated magnitudes and every corrected smoke series lies **below 1°**. The
bound itself is therefore mis-scaled, not the measurement. **No change to the bound is made
here.** The choice — (a) amend the sanity range to the corrected units before the full run is
read, documented as a units correction unrelated to any outcome, or (b) leave it, report the
full run as descriptive only, and re-run under a new pre-registration — is recorded as an open
decision for the author on card t_40524ff0. Gate definitions (A1, A2, C) are unchanged either way.
The rod/ODE ratio column in the smoke table inherits the same factor (the scalar amplitude
κe0 was derived from the inflated bend) and is likewise not read.

## Amendment — adopted 2026-09-12 by the author (option (a)), before any full-run output existed

Reason: the sanity bound "Cobb in O(1–40°)" above was written against the 2026-09-03 run's
48×-inflated reporting scale. It is a units error in the bound, not a property of the rod. The
amendment changes only the magnitude window of the sanity block; **Gates A1, A2 and C are
unchanged in definition.** At the time of this commit the raw output file is still the
2026-09-03 artifact (the 180-month re-run, PID 994575, is in its developmental batch and has
written nothing), so no gate value has been seen by anyone.

Amended sanity block (must hold or the run is discarded, not interpreted):
- **Total absolute planar bend** (the quantity the `cobb` key stores; not clinical Cobb) between
  **0.05° and 10°** at every observation, in every world, experiment and control. Rationale: the
  0.5° seed tilt puts the stiffest column (`B_g` 0.5) near 0.22° in the corrected smoke; 10° is an
  order of magnitude above the largest corrected smoke value (1.17°), so anything outside is a
  measurement or solver failure.
- `max |t_y|` < 1e-3 (unchanged).
- Absolute control drift, |control last − control first| per world, < 5° (unchanged; this bound
  was derived from the seed tilt and was never on the inflated scale).
- A2 and the drift are evaluated over the **recorded window** (first sample at 5.083 y, after
  month 1; no age-5 baseline sample exists in this output) — acknowledged limitation, reported
  by the reader as such.

Reader: `scripts/experiments/newton/ratchet_rod_readout.py` applies these bounds by default and
records them in `readout.json`; `--bend-bounds 1 40` reproduces the pre-amendment reading for
the record. Git timestamp of this amendment = the commit that adds this section.

## Reading of the 180-month re-run (2026-09-12, under the amendment above)

Run: PID 994575, 13:12–16:09 IST, `run.log` exit=0; A1 306 s, control 5190 s, experiment 5106 s.
Raw output `ratchet_rod.json` (SHA256 `15bd31b5…d450b8`) was written by the still-loaded schema-1
code, so its `cobb` column and the raw `ratchet_rod.md` are 48×-inflated; the corrected view is
`reports/newton_readout_audit_2026-09-12/full/{readout.json,READOUT.md}` from
`ratchet_rod_readout.py` (bounds 0.05–10°, recorded window 5.083–20 y). Gate A1: PASS.
`max |t_y|` = 0 in every world (planar). Corrected κe0 from the control columns: 0.0212 rad/m —
essentially the scalar model's assumed 0.02.

| B_g | k_r | bend last (°) | permanent bend (°) | control drift (°) | rod ⟨κp⟩ (rad/m) | scalar ODE κp | rod/ODE | sanity | A2 (recorded window) |
|---|---|---|---|---|---|---|---|---|---|
| 0.5 | 0.3 | 1.81 | 1.18 | 0.0005 | 0.0432 | 0.0634 | 0.68 | pass | pass |
| 0.5 | 1.0 | 0.95 | 0.54 | 0.0005 | 0.0198 | 0.0369 | 0.54 | pass | pass |
| 0.5 | 6.0 | 0.37 | 0.11 | 0.0005 | 0.0041 | 0.0096 | 0.43 | pass | pass |
| 0.3 | 0.3 | 9.33 | 5.15 | 0.024 | 0.1877 | 0.0634 | 2.96 | pass | pass |
| 0.3 | 1.0 | 3.48 | 1.74 | 0.024 | 0.0633 | 0.0369 | 1.72 | pass | pass |
| 0.3 | 6.0 | 0.95 | 0.27 | 0.024 | 0.0098 | 0.0096 | 1.03 | pass | pass |
| 0.2 | 0.3 | 86.51 | 45.76 | 0.365 | 1.6667 | 0.0634 | 26.3 | **fail (>10°)** | (pass) |
| 0.2 | 1.0 | 27.89 | 10.50 | 0.365 | 0.3825 | 0.0369 | 10.4 | **fail (>10°)** | (pass) |
| 0.2 | 6.0 | 3.34 | 0.82 | 0.365 | 0.0298 | 0.0096 | 3.11 | pass | pass |

**Verdict, per B_g, as the pre-registration requires (mixed outcome; no post-hoc selection):**

- **B_g = 0.5 — Gate C PASS.** Sanity and A2 hold; ⟨κp⟩ orders 0.0432 > 0.0198 > 0.0041 for
  k_r 0.3 > 1.0 > 6.0. The rod cements about half of what the scalar law predicts (rod/ODE 0.43–0.68).
- **B_g = 0.3 — Gate C PASS.** Sanity holds (largest bend 9.33°, under the 10° ceiling), A2 holds;
  0.1877 > 0.0633 > 0.0098. Here the rod cements 1–3× the scalar prediction.
- **B_g = 0.2 — not readable under the adopted sanity bound.** The law-on columns at k_r 0.3 and
  1.0 reach 86.5° and 27.9° of total bend; the control column drifts only 0.365°, so this is not a
  measurement failure but the ratchet driving a near-critical column (B_g crit 0.136) into runaway
  set — more set, more sag, more set — a feedback the scalar ODE does not contain. The
  pre-registered rule is exclusion, not tuning, and the descriptive order (also 0.3 > 1.0 > 6.0)
  is **not** read as a gate. The reader's whole-run status is therefore
  `PREREQUISITE_FAILURE_NO_MECHANISTIC_VERDICT`; no single verdict is claimed for the run.
- A2 is certified only over the recorded window (first sample after month 1; no age-5 baseline),
  as the amendment states.

**What this does and does not say.** At the two stiffnesses where the pre-registered sanity block
holds, a spatially resolved rod with per-joint Hueter–Volkmann cementing driven by local elastic
curvature reproduces the scalar ratchet's ordering by restoration rate. It also shows a B_g
dependence the scalar model lacks (rod/ODE rising from ~0.5 at B_g 0.5 to ~2 at 0.3 to ≥10 at
0.2), i.e. proximity to the passive buckling threshold amplifies cementing — an observation, not
a pre-registered prediction, to be pre-registered before any weight is put on it. Nothing here is
clinical: the rod has no vertebrae, discs, muscles, ribs or growth plates, and the manuscript
cites none of it. Lesson for the next protocol: bound the **control** column's bend for
measurement validity and pre-register the law-on column's runaway near B_g crit as the
prediction under test, instead of a single ceiling on both.

### Independent confirmation and a caveat on the B_g = 0.2 interpretation (Codex, 2026-09-12)

`reports/newton_readout_audit_2026-09-12/full/independent/CONFIRMATION.md` recomputed all 66
numeric and gate fields of the readout from the raw JSON (same SHA) and matches them. It adds a
caveat the reading above states too confidently: "runaway cementing" at B_g 0.2 is a
**mechanistic hypothesis, not an established mechanical instability**, until timestep, VBD
iteration and mesh convergence are checked — a stable law-off control and zero out-of-plane
tangent narrow the explanation but do not prove the high-bend law-on solution is numerically
converged, and a terminal magnitude (84.7 → 86.5° over the last 2.4 y) does not establish
divergence. The first sampled ceiling crossings fall in the 11.58–12.08 y (k_r 0.3) and
12.08–12.58 y (k_r 1.0) intervals. The proposed convergence protocol (separate output directory,
frozen before any new run; B_g 0.2/k_r 0.3 plus a B_g 0.3/k_r 0.3 reference with matched
controls; DT, iterations and segment count varied one at a time with forcing exposure per
modelled year held fixed; per-joint state saved; agreement criteria pre-declared) is recorded on
the board as the next step. The per-B_g verdicts above are unchanged.
